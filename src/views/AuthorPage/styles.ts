import { Theme } from "@material-ui/core/styles/createMuiTheme";
export const Styles = (theme: Theme) => ({
  root: {
    width: "70%",
    margin: "30px auto",
  },
  intro: {},
  name: {
    fontWeight: 600,
    fontSize: 30,
  },
  subTitle: {
    fontSize: 20,
    fontWeight: 600,
    marginBottom: 20,
  },
  booksList: {
    marginTop: 30,
  },
});
